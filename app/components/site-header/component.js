import Ember from 'ember';

export default Ember.Component.extend({
  //fullName: Ember.computed('fullName', {
  //  get() {
  //    return this.get('model.fullName');
  //  },
  //  set(key, value) {
  //    this.set('model.fullName', value);
  //  }
  //})
});
